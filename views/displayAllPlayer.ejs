<div>
    <% if(rs.length > 0) { %>
    <table border="1">
        <tr><th>Name</th></tr>
        <% for (var i=0; rs.length > i; i++) { %>
        <tr>
            <td><a href="/Player/?id=<%= rs[i].id %>"> <%= rs[i].name %></a></td>
        </tr>
        <% } %>
    </table>
    <% } else { %>
    No Players are entered into the Player database table.
    <% } %>
</div>

<div style="margin-top:10px;">
    <a href="/Player/create">Create New Guardian</a>
</div>