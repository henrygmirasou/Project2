<div>
    <% if(rs.length > 0) { %>
    <table border="1">
        <tr><th>Name</th></tr>
        <% for (var i=0; rs.length > i; i++) { %>
        <tr>
            <td><a href="/Faction/?id=<%= rs[i].id %>"> <%= rs[i].name %></a></td>
        </tr>
        <% } %>
    </table>
    <% } else { %>
    No Factions are entered into the Faction database table.
    <% } %>
</div>

<div style="margin-top:10px;">
    <a href="/Faction/create">Create New Faction</a>
</div>