<div>
    <% if(rs.length > 0) { %>
    <table border="1">
        <tr><th>Name</th></tr>
        <% for (var i=0; rs.length > i; i++) { %>
        <tr>
            <td><a href="/Armor/?id=<%= rs[i].id %>"> <%= rs[i].name %></a></td>
        </tr>
        <% } %>
    </table>
    <% } else { %>
    No Armor are entered into the Armor database table.
    <% } %>
</div>

<div style="margin-top:10px;">
    <a href="/Armor/create">Create New Armor</a>
</div>